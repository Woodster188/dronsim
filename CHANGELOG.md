# Changelog - История изменений

## [1.2.0] - Управление с клавиатуры

### ✨ Новая функция: Интерактивное управление дроном

**Добавлено управление с клавиатуры:**
- ⌨️ Клавиша **K** - включение/выключение ручного управления
- **WASD** - движение в горизонтальной плоскости (XZ)
- **Space/Shift** - изменение высоты (Y)

**Как это работает:**
```
Клавиши → Целевая позиция → PID контроллер → Стабилизация
```

Система управляет **целевой позицией**, а не двигателями напрямую:
- Плавное движение без рывков
- Автоматическая стабилизация
- Безопасные ограничения

**Новые файлы:**
- `js/keyboardControl.js` - класс управления
- `KEYBOARD_CONTROLS.md` - подробная документация

**Изменения в UI:**
- Новый индикатор режима управления (🤖 АВТОПИЛОТ / ⌨️ РУЧНОЕ УПРАВЛЕНИЕ)
- Подсказки по клавишам в панели управления
- Пульсирующая анимация при активном управлении

**Параметры:**
- Скорость горизонтального движения: 2.0 м/с
- Скорость вертикального движения: 1.5 м/с
- Радиус области полета: 15 метров
- Минимальная высота: 0.5 метра

**Интеграция:**
- Работает совместно с демо-режимом
- Работает при любых внешних воздействиях
- Визуализация целевой точки (зеленая сфера движется)

### 🎮 Сценарии использования

1. **Свободный полет**: Запустите симуляцию → нажмите K → летайте!
2. **С ветром**: Установите ветер → почувствуйте сопротивление
3. **Перехват управления**: Запустите демо → нажмите K → измените траекторию

---

## [1.1.2] - КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Преобразование координат тяги

### 🐛 Исправлена фатальная ошибка в преобразовании тяги

**Проблема:**
- После первого толчка дрон падал на землю
- Траектория показывала резкое падение вниз и в сторону
- Система стабилизации не работала

**Причина:**
Неправильная формула преобразования тяги из локальной системы координат дрона в мировую:

```javascript
// БЫЛО (НЕПРАВИЛЬНО):
const thrustX = totalThrust * (Math.sin(yaw) * Math.cos(pitch));
const thrustY = totalThrust * (Math.cos(roll) * Math.cos(pitch));
const thrustZ = totalThrust * (Math.cos(yaw) * Math.sin(pitch));
```

Ошибки:
1. Использовался `yaw` для вычисления `thrustX` и `thrustZ` - неправильно!
2. `yaw` - это поворот вокруг вертикальной оси, не влияет на вертикальную компоненту
3. Формула не соответствовала физике наклона квадрокоптера

**Решение:**

```javascript
// СТАЛО (ПРАВИЛЬНО):
const thrustX = totalThrust * Math.sin(pitch);
const thrustY = totalThrust * Math.cos(pitch) * Math.cos(roll);
const thrustZ = -totalThrust * Math.sin(roll) * Math.cos(pitch);
```

Правильная физика:
- **pitch** (наклон вперед/назад) → создает тягу по оси **X**
- **roll** (наклон влево/вправо) → создает тягу по оси **Z**
- **Вертикальная компонента** уменьшается при любом наклоне: `cos(pitch) * cos(roll)`

### 📊 Результат

**Было:**
- ✅ Каскадное управление реализовано
- ❌ Дрон падает после первого толчка
- ❌ Неправильное преобразование координат

**Стало:**
- ✅ Правильное преобразование координат
- ✅ Дрон стабильно удерживает позицию
- ✅ Наклоны работают как ожидается

### 🎯 Физическое объяснение

Когда дрон наклоняется на угол θ:
```
Вертикальная сила: F_y = F_total * cos(θ)  ← уменьшается
Горизонтальная сила: F_x = F_total * sin(θ)  ← создается
```

При наклоне на 30°:
- 86.6% тяги идет вверх (cos 30°)
- 50% тяги идет горизонтально (sin 30°)

---

## [1.1.1] - КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Каскадное управление позицией

### 🐛 Исправлена критическая ошибка в контроллере

**Проблема:**
- Дрон удерживал высоту (Y), но **не возвращался** к целевой позиции по осям X и Z
- Контроллер вычислял управляющие силы для горизонтального движения, но они **игнорировались**

**Причина:**
- Квадрокоптер не может создавать горизонтальную силу напрямую
- Для горизонтального движения он должен **наклоняться** в нужную сторону
- Отсутствовало преобразование желаемой силы в желаемый угол наклона

**Решение:**
Реализовано **каскадное управление** (cascaded control):

1. **Контур позиции**: ошибка позиции → желаемая сила
2. **Преобразование**: горизонтальная сила → желаемые углы (pitch/roll)
3. **Контур ориентации**: ошибка углов → управляющий момент
4. **Распределение**: моменты → скорости двигателей

**Формулы преобразования:**
```javascript
// Для движения вперед (ось X) → наклон pitch
desiredPitch = atan2(controlForce.x, totalThrust)

// Для движения вбок (ось Z) → наклон roll
desiredRoll = atan2(-controlForce.z, totalThrust)
```

**Ограничения безопасности:**
- Максимальный угол наклона: ±30° (π/6 радиан)
- Предотвращает опасные маневры

### 📊 Результат

**Было:**
- ✅ Высота (Y): удерживается на 2м
- ❌ Позиция X: дрейф, нет возврата
- ❌ Позиция Z: дрейф, нет возврата

**Стало:**
- ✅ Высота (Y): удерживается на 2м
- ✅ Позиция X: возврат к 0
- ✅ Позиция Z: возврат к 0

### 🎯 Теперь в демо-режиме

Дрон:
1. Наклоняется в сторону целевой точки
2. Создает горизонтальную компоненту тяги
3. Движется к центру координат
4. Стабилизируется в точке (0, 2, 0)

---

## [1.1.0] - Улучшенный демо-режим стабилизации

### 🎯 Изменения в демо-режиме

**Четкая цель:**
- Теперь демо-режим явно демонстрирует удержание дрона в точке (0, 2, 0)
- Целевая точка (зеленая сфера) находится точно в начале координат на высоте 2м
- Дрон стартует из этой же точки

**3D воздействия:**

1. **Ветер теперь действует по всем осям:**
   - Горизонтальные компоненты (X, Z) - как раньше
   - **НОВОЕ:** Вертикальная компонента (Y) - восходящие и нисходящие потоки
   - Используется синусоидальная модель для плавных изменений
   - Турбулентность усилена по оси Y

2. **Импульсные толчки в полном 3D:**
   - **БЫЛО:** Толчки в основном в горизонтальной плоскости (±45° по вертикали)
   - **СТАЛО:** Полностью случайное направление в 3D (сферическое распределение)
   - Толчки могут прийти с любой стороны: сверху, снизу, сбоку, под углом

### 📊 Что это дает

**Более реалистичная демонстрация:**
- Показывает работу стабилизации по всем 6 степеням свободы
- Дрон реагирует на воздействия со всех направлений
- Траектория формирует "облако" вокруг целевой точки в 3D

**Лучшая визуализация:**
- Ясно видно, к какой точке дрон пытается вернуться
- Отклонения видны по всем осям
- Векторы сил показывают трехмерные воздействия

### 🔧 Технические улучшения

**ExternalForces.js:**
- Добавлена вертикальная компонента ветра
- Изменена генерация импульсов на сферическую систему координат
- Турбулентность усилена по оси Y (0.8 вместо 0.5)

**Simulation.js:**
- Явная установка целевой позиции в (0, 2, 0)
- Явная установка начальной позиции дрона в (0, 2, 0)
- Улучшенное логирование в консоль

**UI:**
- Обновлен текст подсказки к кнопке демо-режима
- Четко указана цель и типы воздействий

### 💡 Результат

При активации демо-режима вы увидите:
1. Дрон стартует в центре координат (зеленая сфера)
2. Ветер "дует" с разных сторон, включая восходящие потоки
3. Случайные толчки приходят с любых направлений в 3D
4. Дрон постоянно возвращается к целевой точке
5. Траектория показывает все отклонения и возвраты

---

## [1.0.0] - Первый релиз

### ✨ Основные функции
- Физическая модель дрона-квадракоптера
- PID контроллер с функцией Ляпунова
- Система внешних воздействий
- 3D визуализация на Three.js
- Интерактивный UI
- Базовый демо-режим

