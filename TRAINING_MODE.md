# 🎓 Режим обучения (Автоматическая оптимизация параметров)

## Описание

Режим обучения автоматически подбирает оптимальные параметры PID контроллера для максимально быстрой и стабильной стабилизации дрона.

### Что оптимизируется

**6 параметров PID контроллера:**
- `kpPos` - пропорциональный коэффициент для позиции
- `kdPos` - дифференциальный коэффициент для позиции
- `kiPos` - интегральный коэффициент для позиции
- `kpRot` - пропорциональный коэффициент для ориентации
- `kdRot` - дифференциальный коэффициент для ориентации
- `kiRot` - интегральный коэффициент для ориентации

## Как это работает

### Критерий качества (Функция потерь)

Используется **функция Ляпунова** V(x) как мера отклонения от целевого состояния:

```
V(x) = 0.5 * (||position_error||² + ||rotation_error||² +
              ||velocity||² + ||angular_velocity||²)
```

**Комбинированная метрика качества:**

```javascript
Score = 0.4 * средняя_ошибка +
        0.3 * время_стабилизации +
        0.2 * максимальное_отклонение +
        0.1 * остаточная_ошибка
```

Где:
- **Средняя ошибка** - интегральное значение V(t) за тестовый период
- **Время стабилизации** - время до достижения V < 0.5
- **Максимальное отклонение** - max(V(t))
- **Остаточная ошибка** - среднее V в конце теста

**Чем меньше Score, тем лучше!**

### Алгоритм оптимизации

Используется гибридный подход:

**Фаза 1 (итерации 1-10): Random Search**
- Случайный поиск в пространстве параметров
- Помогает исследовать разные области

**Фаза 2 (итерации 11-50): Local Improvement**
- Улучшение лучших найденных параметров
- Добавление шума ±20% для локального поиска

### Условия тестирования

Для каждой комбинации параметров:

1. **Начальное состояние:**
   - Позиция: (2, 3, -1.5) м
   - Скорость: (0, 0, 0) м/с
   - Целевая позиция: (0, 2, 0) м

2. **Внешние воздействия:**
   - Ветер: 6 м/с под углом 45°
   - Толчки: 2 раз/сек, интенсивность 12 Н
   - Без препятствий

3. **Длительность теста:** 10 секунд симуляции

4. **Сбор данных:** Значения функции Ляпунова на каждом шаге

## Использование

### Шаг 1: Запуск обучения

1. Откройте симуляцию в браузере
2. Найдите секцию "🎓 Режим обучения"
3. Нажмите кнопку **"🎓 Начать обучение"**

### Шаг 2: Ожидание результатов

- Будет протестировано **50 комбинаций** параметров
- Прогресс отображается в реальном времени
- Показывается текущий лучший Score
- Можно остановить в любой момент кнопкой "⏹ Остановить"

### Шаг 3: Применение результатов

После завершения появятся кнопки:
- **✅ Применить лучшие** - использовать найденные параметры
- **🔙 Вернуть исходные** - вернуться к исходным параметрам

## Интерпретация результатов

### Таблица результатов

```
Параметр | Исходное | Оптимальное | Изменение
---------|----------|-------------|----------
kpPos    | 10.00    | 12.34       | +23.4%
kdPos    | 5.00     | 6.78        | +35.6%
...
```

### Анализ изменений

**Если kp увеличился:**
- Более быстрая реакция на ошибку
- Может привести к колебаниям, если слишком большой

**Если kd увеличился:**
- Лучшее демпфирование
- Меньше overshoot и колебаний

**Если ki увеличился:**
- Лучше устраняет постоянную ошибку (например, от ветра)
- Может вызвать windup при больших ошибках

### Типичные результаты

**Хороший Score:** < 2.0
- Быстрая стабилизация
- Малые колебания
- Хорошая точность

**Средний Score:** 2.0 - 5.0
- Приемлемая стабилизация
- Могут быть колебания
- Достаточная точность

**Плохой Score:** > 5.0
- Медленная стабилизация
- Большие колебания или неустойчивость
- Низкая точность

## Настройка обучения

В файле `js/trainingMode.js` можно изменить:

### Количество итераций

```javascript
this.totalIterations = 50;  // По умолчанию 50
```

Больше итераций = лучше результат, но дольше времени.

### Длительность теста

```javascript
this.testDuration = 10;  // секунды
```

Короче = быстрее обучение
Длиннее = точнее оценка стабилизации

### Диапазоны поиска

```javascript
this.paramRanges = {
    kpPos: { min: 5, max: 20 },
    kdPos: { min: 2, max: 10 },
    // ...
};
```

Можно сузить диапазон, если знаете примерные значения.

### Веса в метрике

```javascript
const score =
    0.4 * integral / lyapunovValues.length +
    0.3 * settlingTime / lyapunovValues.length +
    0.2 * maxValue +
    0.1 * finalError;
```

Настройте веса в зависимости от приоритетов:
- Важна скорость → увеличьте вес `settlingTime`
- Важна точность → увеличьте вес `finalError`
- Важна стабильность → увеличьте вес `maxValue`

## Практические советы

### 1. Начните с обучения

Сначала запустите обучение с параметрами по умолчанию:
```
1. Нажмите "🎓 Начать обучение"
2. Дождитесь завершения (1-2 минуты)
3. Примените найденные параметры
4. Запустите демо-режим для проверки
```

### 2. Итеративное улучшение

Если результат не идеален:
```
1. Примените найденные параметры
2. Запустите обучение снова
3. Алгоритм будет искать вокруг новых значений
4. Повторите 2-3 раза для тонкой настройки
```

### 3. Разные условия

Оптимизируйте для разных сценариев:
- **Без ветра**: уменьшите windSpeed в коде
- **Сильный ветер**: увеличьте windSpeed
- **Агрессивный полет**: уменьшите testDuration до 5 сек
- **Точный полет**: увеличьте testDuration до 15 сек

### 4. Проверка результатов

После применения параметров:
```
1. Запустите демо-режим
2. Наблюдайте за поведением
3. Смотрите на функцию Ляпунова
4. Если V быстро уменьшается → хорошие параметры
```

## Сравнение с ручной настройкой

| Аспект | Ручная настройка | Автоматическая |
|--------|------------------|----------------|
| Время | Часы | Минуты |
| Качество | Зависит от опыта | Объективно оптимально |
| Адаптивность | Нужна перенастройка | Легко переобучить |
| Понимание | Глубокое | Черный ящик |

**Рекомендация:** Используйте автоматическую оптимизацию для быстрого старта, затем тонко настраивайте вручную, понимая физику.

## Ограничения

### Локальные минимумы

Алгоритм может застрять в локальном минимуме. Решение:
- Запустите обучение несколько раз
- Измените диапазоны поиска
- Увеличьте количество итераций

### Специфичность условий

Параметры оптимизируются для конкретных условий (ветер 6 м/с, толчки 2 раз/сек). Если условия сильно изменятся, может потребоваться переобучение.

### Время выполнения

50 итераций × 10 секунд = ~8-10 минут реального времени (с учетом накладных расходов).

## Продвинутые техники

### Многоцелевая оптимизация

Если нужно оптимизировать для нескольких сценариев:
```javascript
// В evaluateParams тестируйте несколько условий
const score1 = await testWithWind(5);    // слабый ветер
const score2 = await testWithWind(10);   // сильный ветер
const finalScore = 0.5 * score1 + 0.5 * score2;
```

### Байесовская оптимизация

Для более эффективного поиска можно интегрировать библиотеку байесовской оптимизации (например, BayesOpt.js).

### Эволюционные алгоритмы

Можно реализовать CMA-ES или генетический алгоритм для более надежного поиска глобального минимума.

## Вопросы и ответы

**Q: Почему Score увеличивается во время обучения?**
A: Алгоритм исследует пространство параметров. Промежуточные результаты могут быть хуже, это нормально.

**Q: Можно ли сохранить найденные параметры?**
A: Параметры отображаются в таблице результатов. Скопируйте их и вставьте в код или используйте кнопку "Применить".

**Q: Как изменить условия тестирования?**
A: Отредактируйте метод `setupTestConditions()` в `js/trainingMode.js`.

**Q: Параметры нестабильны в реальной симуляции?**
A: Возможно, условия тестирования отличаются от реальных. Переобучите с реалистичными условиями.

---

**Удачи в оптимизации! 🎓🚁**

