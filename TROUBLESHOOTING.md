# 🔧 Решение проблем

## Проблема: Кнопка "ДЕМО-РЕЖИМ" не работает

### Шаг 1: Откройте консоль разработчика

**Windows/Linux:** `F12` или `Ctrl + Shift + I`
**macOS:** `Cmd + Option + I`

### Шаг 2: Проверьте наличие ошибок

Перезагрузите страницу и проверьте консоль на наличие красных ошибок.

#### Возможные ошибки и решения:

**1. "Cannot read property 'addEventListener' of null"**
```
Решение: Кнопка не найдена в DOM.
- Проверьте, что в index.html есть: <button id="demoBtn">
- Убедитесь, что скрипты загружаются после DOM
```

**2. "simulation is not defined"**
```
Решение: Симуляция не инициализирована.
- Проверьте, что Three.js загружен
- Откройте консоль и введите: window.simulation
- Должен появиться объект Simulation
```

**3. "startDemoMode is not a function"**
```
Решение: Метод не определен.
- Убедитесь, что все файлы JS загружены
- Проверьте файл js/simulation.js
```

### Шаг 3: Тестовый файл

Откройте `test.html` в браузере для диагностики базовой функциональности кнопки.

### Шаг 4: Ручная проверка в консоли

Откройте консоль браузера и введите:

```javascript
// Проверка 1: Существует ли кнопка?
console.log('Кнопка:', document.getElementById('demoBtn'));

// Проверка 2: Существует ли симуляция?
console.log('Симуляция:', window.simulation);

// Проверка 3: Существует ли метод?
console.log('Метод startDemoMode:', typeof window.simulation?.startDemoMode);

// Проверка 4: Попробуйте вызвать метод вручную
window.simulation.startDemoMode();
```

### Шаг 5: Проверка загрузки файлов

В консоли должны быть сообщения:
```
🚁 Инициализация симуляции дрона...
✅ Готово! Нажмите "Старт" для запуска симуляции.
```

Если их нет:
1. Проверьте пути к JS файлам в index.html
2. Проверьте, что Three.js загружен
3. Откройте вкладку Network (Сеть) и убедитесь, что все файлы загрузились (статус 200)

### Шаг 6: Проверка type="module"

Если используете локальный файл (file://), некоторые браузеры блокируют модули ES6.

**Решение:**
Запустите локальный сервер:
```bash
# Python
python -m http.server 8000

# Node.js
npx http-server -p 8000
```

Затем откройте: `http://localhost:8000`

### Шаг 7: Принудительное добавление обработчика

Если ничего не помогло, добавьте в конец файла `index.html` перед `</body>`:

```html
<script>
    // Ждем загрузки симуляции
    setTimeout(() => {
        const demoBtn = document.getElementById('demoBtn');
        if (demoBtn && window.simulation) {
            demoBtn.addEventListener('click', () => {
                console.log('🎯 Кнопка демо нажата (прямой обработчик)');
                if (window.simulation.isDemoMode) {
                    window.simulation.exitDemoMode();
                    demoBtn.textContent = '🎯 ДЕМО-РЕЖИМ СТАБИЛИЗАЦИИ';
                    demoBtn.classList.remove('active-demo');
                } else {
                    window.simulation.startDemoMode();
                    demoBtn.textContent = '🔙 ВЫЙТИ ИЗ ДЕМО-РЕЖИМА';
                    demoBtn.classList.add('active-demo');
                }
            });
            console.log('✅ Обработчик демо-кнопки добавлен вручную');
        } else {
            console.error('❌ Не найдено:', { demoBtn, simulation: window.simulation });
        }
    }, 1000);
</script>
```

### Шаг 8: Проверка версии браузера

Убедитесь, что используете современный браузер:
- Chrome 60+
- Firefox 60+
- Edge 79+
- Safari 11+

### Шаг 9: Кеш браузера

Очистите кеш и перезагрузите:
- **Windows/Linux:** `Ctrl + Shift + R` или `Ctrl + F5`
- **macOS:** `Cmd + Shift + R`

### Шаг 10: Альтернативный способ запуска

Вместо кнопки, используйте консоль:
```javascript
window.simulation.startDemoMode();
```

---

## Если проблема решена

Сообщите, какой шаг помог, чтобы мы могли улучшить код!

## Если ничего не помогло

Пришлите следующую информацию:
1. Вывод консоли (скриншот или текст)
2. Браузер и версия
3. Как открываете файл (file:// или http://localhost)
4. Результат команды из Шага 4

